import React from 'react';
import PropTypes from 'prop-types';
import SideBar from '~/components/SideBar';

Live.propTypes = {
    
};

function Live(props) {
    return (
        <div>
           <h2>Live</h2>
        </div>
    );
}

export default Live;